# AquaSuite Playwright

This project contains a structured Playwright framework for testing AquaSuite flows across authentication, tenant management, subscriptions, team access, billing, and auditing.

## Quick start

```bash
npm install
npm test
```

## Environment variables

Copy the sample environment values from the files in `configs/env/` and configure the target URL for each environment.
